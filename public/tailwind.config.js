module.exports = {
  //so to purge just uncomment the below line and comment line number 5 then in the console run the 'npm run custom'
  //  purge: { enabled: true, content: ['build/*.html', 'build/*.js'] },
  purge: [],
  theme: {
    extend: {
      fontFamily: {
        RockSalt: ['Rock Salt'],
        Risque: ['Risque'],
      },
    },
  },
  variants: {},
  plugins: [],
};
